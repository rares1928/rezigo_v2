import React from 'react';
import { Typography } from '@material-ui/core';

export default function AdminsHomePage() {
    return(
        <Typography variant="h2">Smecherie pe vampirie</Typography>
    );
}